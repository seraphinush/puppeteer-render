{
  "name": "puppeteer-render",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=local dotenv_config_path=.env.local nodemon -r dotenv/config src/server.js",
    "prod": "cross-env NODE_ENV=production dotenv_config_path=.env.production node -r dotenv/config src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.1",
    "puppeteer": "^23.4.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
